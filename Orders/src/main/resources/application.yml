server:
  port: 8082
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food_app
    password: password
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
